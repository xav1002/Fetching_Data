Create folder for project

npm init --yes

npm install mongoose --save

Create connection.js

node [foldername]\[connection.js]

Create models folder

Create example.js in models

npm install mocha --save

Create test.js

    describe('[test description]', function() {

        // it()'s describe single tests
        it('[description]', function() {
            // Assertion assumes statement to be true, and is successful if it is
            assert([assertion]);
        })
    });